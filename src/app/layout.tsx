import { type Metadata } from 'next'
import {
    ClerkProvider,
    SignInButton,
    SignUpButton,
} from '@clerk/nextjs'


import '@/app/styles/globals.css'
import logo from '@/app/Images/logo.png'
import Link from 'next/link'
import Image from 'next/image'


export const metadata: Metadata = {
    title: 'SPARK: Reigniting learning and innovation',
    description: 'Generated by create next app',
}

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode
}>) {
    return (
        <ClerkProvider publishableKey={process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}>
            <html lang='en'>
                <body>
                    <nav>
                        <ul>
                            <Link href="/"><Image src={logo} alt='SPARK logo' /></Link>

                            <li><a href="https://www.rva.gov/parks-recreation/powhatan-community-center">Contact Us</a></li>
                            <li><a href="/sessions">Events & Sessions</a></li>
                            <li style={{ float: 'right' }}><a><SignInButton /></a></li>
                            <li style={{ float: 'right' }}><a><SignUpButton /></a></li>
                        </ul>
                    </nav>
                    <main>{children}</main>
                </body>
            </html>
        </ClerkProvider>
    );
}
