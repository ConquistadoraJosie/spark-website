import { type Metadata } from 'next'
import {
    ClerkProvider,
    SignInButton,
    SignUpButton,
    SignedIn,
    SignedOut,
    UserButton,
} from '@clerk/nextjs'
import '@/app/styles/globals.css'
import Link from 'next/link'
import Image from 'next/image'
import logo from '@/app/Images/logo.jpg'

export const metadata: Metadata = {
    title: 'SPARK: Reigniting learning and innovation',
    description: 'Generated by create next app',
}
export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode
}>) {
    return (
        <>
            <ClerkProvider publishableKey={process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}>
                <ul className="navbar">
                    <li><Link href="/"><Image src={logo} alt="SPARK Logo" width={50} height={60} style={{ transform: 'translate(0px, -15px)' }} /></Link></li>
                    <li style={{ transform: 'translate(-15px, -5px)' }}><h1 className='h1'>SPARK</h1></li>
                    <li><a href="/mission ">Mission</a></li>
                    <li><a href="https://www.rva.gov/parks-recreation/powhatan-community-center">Contact Us</a></li>
                    <li><a href="/sessions">Sessions & Coming Events</a></li>
                    <li style={{ float: 'right' }}>
                        <a style={{ gap: '16px' }}>
                            <SignedOut>
                                <SignInButton />
                                <SignUpButton />
                            </SignedOut></a></li>
                    <li style={{ float: 'right' }}>
                        <SignedIn>
                            <UserButton />
                        </SignedIn> </li>
                </ul>
                <html lang="en">
                    <body>  {children}</body>

                </html>
            </ClerkProvider >

        </>
    )
} 
